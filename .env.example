# FizzBuzz API Configuration
# Copy this file to .env and modify as needed

# ===========================================
# Database Configuration (PostgreSQL)
# ===========================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fizzbuzz
DB_USER=fizzbuzz_user
DB_PASSWORD=fizzbuzz_pass
DB_SSL_MODE=disable

# Connection Pool Settings
DB_MAX_CONNECTIONS=25
DB_MAX_IDLE_CONNECTIONS=5
DB_CONN_MAX_LIFETIME=5m

# ===========================================
# API Configuration
# ===========================================
API_PORT=4000
API_ENV=development

# ===========================================
# Rate Limiting
# ===========================================
RATE_LIMITER_ENABLED=true
RATE_LIMITER_RPS=10        # Requests per second
RATE_LIMITER_BURST=20      # Burst capacity

# ===========================================
# Statistics & Caching
# ===========================================
CACHE_REFRESH_INTERVAL=30s
STATISTICS_BATCH_SIZE=100
BACKGROUND_WRITE_BUFFER=1000

# ===========================================
# Development Tools (Optional)
# ===========================================
# pgAdmin Configuration
# Note: Change these credentials for any non-local environment
PGADMIN_EMAIL=admin@fizzbuzz.local
PGADMIN_PASSWORD=fizzbuzz_admin_2025

# ===========================================
# Production Overrides
# ===========================================
# For production deployment, override these:
# API_ENV=production
# RATE_LIMITER_RPS=100
# DB_MAX_CONNECTIONS=50
# DB_SSL_MODE=require

# Production Database Security
# Use strong passwords in production:
# DB_PASSWORD=<strong-random-password>
# PGADMIN_PASSWORD=<strong-random-password>

# SSL Configuration for Production
# DB_SSL_MODE=require  # Enable SSL for database connections